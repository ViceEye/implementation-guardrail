interface:
  display_name: "Implement Before Testing"
  short_description: "Finish implementation, then wait before testing"
  default_prompt: "Use $implement-before-testing to implement the approved design and stop after static review."

policy:
  allow_implicit_invocation: true
